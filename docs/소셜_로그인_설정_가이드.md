# 소셜 로그인 설정 및 개선 작업 가이드

> 작성일: 2026-02-05
> Bundle ID: `com.wpipainfree.connect`

---

## 목차
1. [Google 소셜 로그인 재설정](#1%EF%B8%8F⃣-google-소셜-로그인-재설정)
2. [Kakao 소셜 로그인 Key Hash](#2%EF%B8%8F⃣-kakao-소셜-로그인-key-hash)
3. [보안 이슈 수정 (백엔드)](#3%EF%B8%8F⃣-보안-이슈-수정-백엔드)
4. [플랫폼별 소셜 로그인 UI 제어](#4%EF%B8%8F⃣-플랫폼별-소셜-로그인-ui-제어)
5. [마이페이지 소셜 연동 UI 개선](#5%EF%B8%8F⃣-마이페이지-소셜-연동-ui-개선)
6. [로그인 후 네비게이션 수정](#6%EF%B8%8F⃣-로그인-후-네비게이션-수정)
7. [핵심 설정 정보](#-핵심-설정-정보)
8. [체크리스트](#-체크리스트)

---

## 1️⃣ Google 소셜 로그인 재설정

### 문제
- Firebase SHA-1 충돌 에러 발생
- 구 패키지명(`com.wpipainfree.wpiapp`)과 새 패키지명(`com.wpipainfree.connect`)의 SHA-1 충돌

### 해결 과정

#### 1. 새로운 debug.keystore 생성

```bash
# 기존 keystore 백업
cp ~/.android/debug.keystore ~/.android/debug.keystore.backup

# 기존 keystore 삭제
rm ~/.android/debug.keystore

# Flutter 빌드로 새로운 keystore 자동 생성
cd /Users/wilson/WebstormProjects/interpreter_front_flutter
flutter build apk --debug
```

#### 2. 새로운 SHA-1 지문 확인

```bash
/opt/homebrew/opt/openjdk@17/bin/keytool -list -v \
  -keystore ~/.android/debug.keystore \
  -alias androiddebugkey \
  -storepass android \
  -keypass android | grep "SHA1:"
```

**결과:**
```
SHA-1: 49:AD:F6:9F:AB:21:AC:83:43:59:B5:2D:6A:71:94:E7:96:35:45:4A
SHA-256: D3:E9:B3:80:7F:83:F8:74:9B:34:C3:5A:74:3C:14:5A:B9:5D:1E:CC:59:DF:66:10:B8:B1:49:F7:28:2F:64:4F
```

#### 3. 새로운 Firebase 프로젝트

- **Project ID**: `presence-app-social-logi-ea437`
- **Project Number**: `379662490584`
- **Package Name**: `com.wpipainfree.connect`

#### 4. Firebase Console 설정

1. [Firebase Console](https://console.firebase.google.com/project/presence-app-social-logi-ea437/settings/general) 접속
2. "내 앱" → Android 앱 (`com.wpipainfree.connect`) 선택
3. "SHA 인증서 지문" → **지문 추가**
4. 새로운 SHA-1 입력: `49:AD:F6:9F:AB:21:AC:83:43:59:B5:2D:6A:71:94:E7:96:35:45:4A`
5. `google-services.json` 다운로드

#### 5. OAuth 클라이언트 ID

| 플랫폼 | Client ID |
|--------|-----------|
| **Android** | `379662490584-ooavc3rns6mueksieh3qjrogcn9p5rj9.apps.googleusercontent.com` |
| **iOS** | `379662490584-hqc7se783r80cl5ce4eg6smt7lashact.apps.googleusercontent.com` |

#### 6. 파일 업데이트

**Android (`android/app/google-services.json`):**
```json
{
  "project_info": {
    "project_number": "379662490584",
    "project_id": "presence-app-social-logi-ea437"
  },
  "client": [{
    "client_info": {
      "mobilesdk_app_id": "1:379662490584:android:4c08b3134e8c5f1dab70ad",
      "android_client_info": {
        "package_name": "com.wpipainfree.connect"
      }
    },
    "oauth_client": [{
      "client_id": "379662490584-ooavc3rns6mueksieh3qjrogcn9p5rj9.apps.googleusercontent.com",
      "client_type": 1,
      "android_info": {
        "package_name": "com.wpipainfree.connect",
        "certificate_hash": "49adf69fab21ac834359b52d6a7194e79635454a"
      }
    }]
  }]
}
```

**iOS (`ios/Runner/Info.plist`):**
```xml
<key>GIDClientID</key>
<string>379662490584-hqc7se783r80cl5ce4eg6smt7lashact.apps.googleusercontent.com</string>

<key>CFBundleURLSchemes</key>
<array>
    <string>com.googleusercontent.apps.379662490584-hqc7se783r80cl5ce4eg6smt7lashact</string>
</array>
```

---

## 2️⃣ Kakao 소셜 로그인 Key Hash

### 새로운 Kakao Key Hash

```
Sa32n6shrINDWbUtanGU55Y1RUo=
```

### 등록 방법

1. [Kakao Developers](https://developers.kakao.com/) 로그인
2. "내 애플리케이션" → 해당 앱 선택
3. 좌측 메뉴: **앱 설정 → 플랫폼**
4. **Android** 섹션 → **키 해시** 항목
5. 새로운 Key Hash 추가: `Sa32n6shrINDWbUtanGU55Y1RUo=`
6. **패키지명** 확인: `com.wpipainfree.connect`
7. **저장**

### 주의사항

- 이 Key Hash는 **Debug 빌드**용입니다
- **Release 빌드** 시 별도의 Key Hash가 필요합니다
- Release Key Hash 생성 방법:
  ```bash
  keytool -exportcert -alias <release-key-alias> \
    -keystore <release.keystore> | \
    openssl sha1 -binary | \
    openssl base64
  ```

---

## 3️⃣ 보안 이슈 수정 (백엔드)

### 🚨 발견된 보안 취약점

**문제:** 이메일만 일치하면 소셜 계정을 자동으로 연동하여 로그인 허용

**위험도:** 높음 (계정 탈취 가능)

**시나리오:**
1. 사용자 A가 `test@example.com`으로 일반 회원가입
2. 공격자가 같은 이메일을 사용하는 Google 계정으로 로그인 시도
3. 자동으로 Google 계정이 사용자 A의 계정에 연동되고 로그인됨 ⚠️

### 수정 내용

**적용 범위:** Kakao, Google, Apple 모두

#### 파일 1: `app/services/kakao_oauth_service.py`

**이전 코드 (취약):**
```python
user = user_repository.get_by_email(db, email=email)
if user:
    # 자동 연동 처리
    user_social_repository.create(
        db=db,
        user_id=user.ID,
        provider="kakao",
        provider_user_id=kakao_id,
        email=email,
        nickname=name
    )
    return user  # ❌ 로그인 허용
```

**수정된 코드 (안전):**
```python
user = user_repository.get_by_email(db, email=email)
if user:
    # 이메일은 같으나 연동이 안 된 상태 -> 로그인 거부
    logger.warning(f"카카오 로그인 - 이메일은 존재하나 카카오 계정 미연동")
    logger.warning(f"보안상 자동 연동을 허용하지 않습니다.")
    return None  # ✅ 로그인 거부
```

#### 파일 2: `app/services/social_oauth_service.py`

**수정된 코드:**
```python
user = user_repository.get_by_email(db, email=email)
if user:
    # 보안: 이메일이 같아도 소셜 계정이 연동되지 않았으면 로그인 불가
    logger.warning(f"{cfg.name} 로그인 - 이메일은 존재하지만 소셜 연동 안됨")
    return None  # ✅ 로그인 거부
```

### 올바른 플로우

1. **소셜 로그인 시도**
2. **이메일 일치 확인**
3. **소셜 계정 연동 확인**
   - 연동됨 → ✅ 로그인 허용
   - 연동 안됨 → ❌ 로그인 거부 + "등록되지 않은 사용자입니다" 메시지
4. **사용자가 수동으로 연동**
   - 로그인 후 마이페이지에서 소셜 계정 연동

---

## 4️⃣ 플랫폼별 소셜 로그인 UI 제어

### 파일: `lib/screens/auth/login_screen.dart`

### 요구사항

| 플랫폼 | 카카오 | Google | Apple |
|--------|--------|--------|-------|
| **Android** | ✅ 표시 | ✅ 표시 | ❌ 숨김 |
| **iOS** | ✅ 표시 | ✅ 표시 | ✅ 표시 |

### 구현

```dart
import 'dart:io' show Platform;
import 'package:flutter/material.dart';

// ...

if (socialEnabled) ...[
  // 카카오 (모든 플랫폼)
  _SocialButton(
    label: '카카오로 계속하기',
    backgroundColor: AppColors.kakao,
    textColor: AppColors.kakaoText,
    icon: Icons.chat_bubble_outline_rounded,
    onPressed: _isLoading ? null : () => _handleSocialLogin('kakao'),
  ),
  const SizedBox(height: 12),

  // Google (모든 플랫폼)
  _SocialButton(
    label: 'Google로 계속하기',
    backgroundColor: Colors.white,
    textColor: AppColors.textPrimary,
    icon: Icons.public,
    borderColor: AppColors.border,
    onPressed: _isLoading ? null : () => _handleSocialLogin('google'),
  ),

  // Apple (iOS만)
  if (Platform.isIOS) ...[
    const SizedBox(height: 12),
    _SocialButton(
      label: 'Apple로 계속하기',
      backgroundColor: Colors.black,
      textColor: Colors.white,
      icon: Icons.apple,
      onPressed: _isLoading ? null : () => _handleSocialLogin('apple'),
    ),
  ],
  const SizedBox(height: 20),
],
```

---

## 5️⃣ 마이페이지 소셜 연동 UI 개선

### 파일: `lib/screens/profile/my_page_screen.dart`

### 개선 1: 플랫폼별 Apple 연동 제어

**요구사항:**
- **Android**: Apple 연동 버튼 잠금 🔒 (iOS 전용 표시)
- **iOS**: Apple 연동 버튼 활성화 ✅

**구현:**

```dart
import 'dart:io' show Platform;

// _SocialAccountLinkTile 위젯에 isEnabled 파라미터 추가
class _SocialAccountLinkTile extends StatelessWidget {
  final String provider;
  final bool isLinked;
  final bool isLoading;
  final bool isEnabled;  // 새로 추가
  final VoidCallback onLinkTap;
  final VoidCallback onUnlinkTap;

  const _SocialAccountLinkTile({
    required this.provider,
    required this.isLinked,
    this.isLoading = false,
    this.isEnabled = true,  // 기본값: 활성화
    required this.onLinkTap,
    required this.onUnlinkTap,
  });

  // ...
}

// Apple 타일에 플랫폼 체크 적용
_SocialAccountLinkTile(
  provider: 'apple',
  isLinked: _isProviderLinked('apple'),
  isLoading: _isProviderLinking('apple'),
  isEnabled: Platform.isIOS,  // ✅ Android에서 비활성화
  onLinkTap: () => _handleLinkSocial(context, 'apple'),
  onUnlinkTap: () => _handleUnlinkSocial(context, 'apple'),
),
```

**UI 변경:**

```dart
subtitle: Text(
  !isEnabled
      ? 'iOS 전용'  // Android에서 표시
      : (isLinked ? '연동 완료' : '연동 안됨'),
  style: AppTextStyles.caption.copyWith(
    color: !isEnabled
        ? AppColors.textSecondary.withOpacity(0.5)
        : (isLinked ? Colors.green : AppColors.textSecondary),
  ),
),
trailing: !isEnabled
    ? Icon(Icons.lock_outline, ...)  // 🔒 잠금 아이콘
    : ... // 기존 버튼
```

### 개선 2: 버튼 정렬 통일

**문제:** 연동/해제/잠금 버튼의 크기가 달라 정렬이 맞지 않음

**해결:** 모든 trailing 위젯을 **70x36** 크기로 통일

```dart
trailing: SizedBox(
  width: 70,
  height: 36,
  child: !isEnabled
      ? Center(
          child: Icon(
            Icons.lock_outline,
            color: AppColors.textSecondary.withOpacity(0.5),
            size: 20,
          ),
        )
      : isLinked
          ? TextButton(
              onPressed: isLoading ? null : onUnlinkTap,
              style: TextButton.styleFrom(
                minimumSize: const Size(70, 36),
                padding: EdgeInsets.zero,
              ),
              child: const Text('해제', style: TextStyle(color: Colors.red)),
            )
          : isLoading
              ? const Center(
                  child: SizedBox(
                    width: 24,
                    height: 24,
                    child: CircularProgressIndicator(strokeWidth: 2),
                  ),
                )
              : ElevatedButton(
                  onPressed: onLinkTap,
                  style: ElevatedButton.styleFrom(
                    backgroundColor: _providerColor(provider),
                    foregroundColor: provider == 'kakao' ? Colors.black : Colors.white,
                    minimumSize: const Size(70, 36),
                    padding: const EdgeInsets.symmetric(horizontal: 12),
                  ),
                  child: const Text('연동'),
                ),
),
```

### 개선 3: 개별 로딩 상태 관리

**문제:** 하나의 버튼 클릭 시 모든 버튼이 로딩 상태가 됨

**원인:** 단일 변수 `_isLinkingSocial`로 모든 버튼의 로딩 상태를 공유

**해결:** 각 provider별로 독립적인 로딩 상태 관리

```dart
// 이전 (문제)
bool _isLinkingSocial = false;

Future<void> _handleLinkSocial(BuildContext context, String provider) async {
  if (_isLinkingSocial) return;
  setState(() => _isLinkingSocial = true);  // ❌ 모든 버튼이 로딩

  final result = await _authService.linkSocialAccountWithSdk(provider);

  setState(() => _isLinkingSocial = false);
  // ...
}

// 수정 후 (해결)
final Map<String, bool> _linkingProviders = {};

bool _isProviderLinking(String provider) {
  return _linkingProviders[provider] ?? false;
}

Future<void> _handleLinkSocial(BuildContext context, String provider) async {
  if (_isProviderLinking(provider)) return;
  setState(() => _linkingProviders[provider] = true);  // ✅ 해당 provider만

  final result = await _authService.linkSocialAccountWithSdk(provider);

  setState(() => _linkingProviders[provider] = false);
  // ...
}

// 각 타일에 해당 provider의 로딩 상태만 전달
_SocialAccountLinkTile(
  provider: 'kakao',
  isLoading: _isProviderLinking('kakao'),  // ✅ kakao만
  // ...
),
_SocialAccountLinkTile(
  provider: 'google',
  isLoading: _isProviderLinking('google'),  // ✅ google만
  // ...
),
_SocialAccountLinkTile(
  provider: 'apple',
  isLoading: _isProviderLinking('apple'),  // ✅ apple만
  // ...
),
```

---

## 6️⃣ 로그인 후 네비게이션 수정

### 파일: `lib/screens/auth/login_screen.dart`

### 문제
- 로그인 성공 후 페이지가 이동하지 않고 로그인 화면에 머물러 있음
- `Navigator.pop(success)`만으로는 메인 페이지로 이동하지 않음

### 해결

```dart
void _completeAuth({required bool success}) {
  if (success) {
    // ✅ 로그인 성공: 메인 페이지로 이동 (이전 화면 모두 제거)
    Navigator.of(context, rootNavigator: true).pushNamedAndRemoveUntil(
      AppRoutes.main,      // 메인 페이지로 이동
      (route) => false,    // 이전 화면 모두 제거 (백버튼 방지)
    );
  } else {
    // 로그인 취소: 이전 화면으로 돌아가기
    Navigator.of(context, rootNavigator: true).pop(success);
  }
}
```

### 개선 사항

| 동작 | 이전 | 수정 후 |
|------|------|---------|
| 로그인 성공 | `pop()` → 이동 안 됨 ❌ | `pushNamedAndRemoveUntil()` → 메인 페이지 ✅ |
| 백버튼 | 로그인 화면으로 돌아감 ❌ | 앱 종료 ✅ |
| 화면 스택 | 스플래시 → 로그인 → 메인 | 메인만 남음 ✅ |

---

## 📝 핵심 설정 정보

### Package/Bundle ID
```
com.wpipainfree.connect
```

### SHA-1 지문 (Android Debug)
```
49:AD:F6:9F:AB:21:AC:83:43:59:B5:2D:6A:71:94:E7:96:35:45:4A
```

### SHA-256 지문 (Android Debug)
```
D3:E9:B3:80:7F:83:F8:74:9B:34:C3:5A:74:3C:14:5A:B9:5D:1E:CC:59:DF:66:10:B8:B1:49:F7:28:2F:64:4F
```

### Kakao Key Hash (Debug)
```
Sa32n6shrINDWbUtanGU55Y1RUo=
```

### Firebase Project
- **Project ID**: `presence-app-social-logi-ea437`
- **Project Number**: `379662490584`
- **Storage Bucket**: `presence-app-social-logi-ea437.firebasestorage.app`

### Google OAuth Client IDs

| 플랫폼 | Client ID |
|--------|-----------|
| **Android** | `379662490584-ooavc3rns6mueksieh3qjrogcn9p5rj9.apps.googleusercontent.com` |
| **iOS** | `379662490584-hqc7se783r80cl5ce4eg6smt7lashact.apps.googleusercontent.com` |
| **iOS URL Scheme** | `com.googleusercontent.apps.379662490584-hqc7se783r80cl5ce4eg6smt7lashact` |

### Kakao Settings
- **Android Package**: `com.wpipainfree.connect`
- **iOS Bundle ID**: `com.wpipainfree.connect`
- **Debug Key Hash**: `Sa32n6shrINDWbUtanGU55Y1RUo=`

---

## ✅ 체크리스트

### Google 로그인
- [x] Firebase Console에 SHA-1 등록
- [x] `google-services.json` 다운로드 및 적용
- [x] iOS `Info.plist` 업데이트 (GIDClientID, URL Scheme)
- [x] 플랫폼별 UI 제어 (Android: 카카오+Google, iOS: 카카오+Google+Apple)
- [x] `android/app/google-services.json` 업데이트
- [x] `auth_service.dart` 확인 (자동 감지)

### Kakao 로그인
- [ ] **⚠️ Kakao Developer Console에 Key Hash 등록 필요**
  - Key Hash: `Sa32n6shrINDWbUtanGU55Y1RUo=`
  - 위치: 앱 설정 → 플랫폼 → Android → 키 해시
- [x] 플랫폼별 UI 제어
- [x] Android 패키지명 확인 (`com.wpipainfree.connect`)

### Apple 로그인
- [x] iOS 전용으로 제한
- [x] Android에서 잠금 처리 (🔒 iOS 전용 표시)
- [x] `Runner.entitlements` 설정
- [x] Info.plist 확인

### 보안
- [x] 백엔드 자동 연동 취약점 제거
  - `kakao_oauth_service.py` 수정
  - `social_oauth_service.py` 수정
- [x] 모든 소셜 로그인(Kakao, Google, Apple)에 적용
- [x] 테스트 시나리오 확인

### UI/UX
- [x] 로그인 성공 후 메인 페이지로 이동
- [x] 버튼 정렬 통일 (70x36)
- [x] 개별 로딩 상태 관리 (각 provider별)
- [x] 플랫폼별 Apple 버튼 제어 (로그인 + 마이페이지)

### 테스트
- [x] Android 에뮬레이터에서 앱 실행
- [ ] Kakao 로그인 테스트 (Key Hash 등록 후)
- [ ] Google 로그인 테스트
- [ ] 로그인 후 메인 페이지 이동 확인
- [ ] 마이페이지 소셜 연동 테스트
- [ ] iOS 시뮬레이터/기기에서 테스트

---

## 🔧 트러블슈팅

### 1. Kakao 로그인 실패: "Android keyHash validation failed"

**원인:** Kakao Developer Console에 Key Hash가 등록되지 않음

**해결:**
1. Kakao Developers → 앱 설정 → 플랫폼 → Android
2. 키 해시 추가: `Sa32n6shrINDWbUtanGU55Y1RUo=`
3. 저장 후 앱 재시작

### 2. Google 로그인 실패: SHA-1 관련 에러

**원인:** Firebase Console에 SHA-1이 등록되지 않았거나 잘못됨

**해결:**
1. Firebase Console 접속
2. 프로젝트 설정 → Android 앱
3. SHA-1 확인: `49:AD:F6:9F:AB:21:AC:83:43:59:B5:2D:6A:71:94:E7:96:35:45:4A`
4. `google-services.json` 재다운로드 및 적용

### 3. 로그인 성공 후 화면 이동 안 됨

**원인:** `_completeAuth` 메서드가 `pop()`만 호출

**해결:**
- `login_screen.dart`의 `_completeAuth` 메서드 수정
- `pushNamedAndRemoveUntil()` 사용

### 4. Release 빌드에서 Kakao/Google 로그인 실패

**원인:** Release 키스토어의 SHA-1/Key Hash가 등록되지 않음

**해결:**
1. Release 키스토어에서 SHA-1 추출
2. Firebase/Kakao Console에 등록
3. Release Key Hash도 별도 생성하여 Kakao에 등록

---

## 📚 참고 자료

### Firebase
- [Firebase Console](https://console.firebase.google.com/)
- [Firebase Android 설정 가이드](https://firebase.google.com/docs/android/setup)
- [Firebase iOS 설정 가이드](https://firebase.google.com/docs/ios/setup)

### Kakao
- [Kakao Developers Console](https://developers.kakao.com/)
- [Kakao 로그인 가이드](https://developers.kakao.com/docs/latest/ko/kakaologin/common)
- [Android 플랫폼 등록](https://developers.kakao.com/docs/latest/ko/getting-started/app#platform-android)

### Google Sign-In
- [Google Cloud Console](https://console.cloud.google.com/)
- [Google Sign-In for Flutter](https://pub.dev/packages/google_sign_in)
- [OAuth 2.0 설정](https://developers.google.com/identity/protocols/oauth2)

### Apple Sign-In
- [Sign in with Apple](https://developer.apple.com/sign-in-with-apple/)
- [Flutter Apple Sign-In](https://pub.dev/packages/sign_in_with_apple)

---

## 📞 연락처

문제가 발생하거나 추가 도움이 필요한 경우 개발팀에 문의하세요.

---

**Last Updated:** 2026-02-05
**Version:** 1.0
**Package:** com.wpipainfree.connect
